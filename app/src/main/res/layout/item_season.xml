<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/textEpisodes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/season_text_margin_verical"
        android:layout_marginStart="@dimen/character_info_text_title_margin_horizontal"
        tools:text="@string/text_episodes"
        android:textColor="@color/gray_400"
        android:textSize="@dimen/text_episodes_size" />

</FrameLayout>